template(name="wingsEditor")
  .wings-editor-wrapper(class="{{saveRemainingClass}} {{activeClass}}")
    .wings-editor(class="{{class}}" placeholder="{{placeholder}}" contenteditable="true" name="editor") {{{fieldValue}}}
    .commands
      .wings-button lưu
