template(name="customerManagement")
  .customerManagement
    .customerManagement-header
      if isActiveHome
        .search-filter
          input(name='searchFilter' type="text" value="{{searchFilter.textSearch}}")
          i.icon-search-8

      else
        ul(style="padding-left: 0; list-style: none;")
          li(style="display: inline-block; cursor: pointer; padding-left: 5px").icon-left-open-big.customerHome Chi Tiết

    .customerManagement-body
      if isActiveHome
        each managedCustomers
          if showFilterSearch
            +customerSummary
      else
        +customerDetail editingData

    .customerManagement-footer