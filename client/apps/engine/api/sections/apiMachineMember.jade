template(name="apiMachineMember")
  .leaf-detail.member
    .leaf-name {{name}}
      span.return-icon.icon-left-circle
      span.return-signature
        if type
          span {{brackets type.name}}
        else
          span {void}
      span.remove-command.icon-cancel-circled-4
    .leaf-doc
      .doc-editor.wings-editor(contenteditable="true" placeholder="Ghi chú...")